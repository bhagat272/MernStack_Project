{
    "version": 2,
    "builds": [
      {
        "src": "api/server.js",
        "use": "@vercel/node"
      },
      {
        "src": "projectmern/package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "projectmern/build"
        }
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/api/server.js"
      },
      {
        "src": "/(.*)",
        "dest": "/projectmern/$1"
      }
    ]
  }
  